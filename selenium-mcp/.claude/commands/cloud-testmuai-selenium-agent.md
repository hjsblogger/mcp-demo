---
description: Run the TestMu AI cloud Chrome+Firefox parallel search-to-checkout Selenium test
---

Follow the steps in `instructions/cloud-testmuai-selenium-agent.md` exactly, using the `selenium-mcp-cloud` MCP server tools. Launch one subagent per browser (Chrome and Firefox), both in the same dispatch, so they run in parallel on the TestMu AI grid. Report a combined pass/fail summary with session IDs and dashboard links, and always close each browser session at the end regardless of outcome.
