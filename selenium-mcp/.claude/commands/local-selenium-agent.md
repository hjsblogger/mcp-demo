---
description: Run the local Chrome iPhone-search Selenium smoke test
---

Follow the steps in `instructions/local-selenium-agent.md` exactly, using the `selenium-mcp-local` MCP server tools. Report pass/fail on the title assertion, and always close the browser session at the end regardless of outcome.
